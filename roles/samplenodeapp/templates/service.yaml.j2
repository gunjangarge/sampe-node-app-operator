apiVersion: v1
kind: Service
metadata:
  labels:
    app: samplenodeapp
  name: {{meta.name}}-service
  namespace: {{meta.namespace}}
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: samplenodeapp
