apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: samplenodeapp
  name: {{meta.name}}
  namespace: {{meta.namespace}}
spec:
  host: ""
  port:
    targetPort: 8080
  to:
    kind: "Service"
    name: {{meta.name}}-service
    weight: null
